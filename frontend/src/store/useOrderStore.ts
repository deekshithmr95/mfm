// Re-export from shared package for backwards compatibility
export { useOrderStore } from '@mfm/shared';
export type { Order, OrderItem, OrderAddress, PaymentMethod, OrderStatus } from '@mfm/shared';
